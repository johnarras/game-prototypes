namespace Genrpg.MapServer.Maps.Constants
{
    public class MapInstanceConstants
    {
        /// <summary>
        /// Simplifies the game server when active (only 1 small map and only 1 message queue for now)
        /// </summary>
        public static bool ServerTestMode = false;
    }
}
